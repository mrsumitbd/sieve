{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "Node", "parent_classes": ["Generic[T]"], "docstring": "A node in a singly linked list.", "source_code": "class Node(Generic[T]):\n    \"\"\"A node in a singly linked list.\"\"\"\n\n    def __init__(self, value: T, next: Optional[Node[T]] = None):\n        self.value = value\n        self.next = next", "skeleton": "class Node(Generic[T]):\n    \"\"\"A node in a singly linked list.\"\"\"\n\n    def __init__(self, value: T, next: Optional[Node[T]] = None):\n        pass\n", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": ["__init__"], "method_count": 1, "has_constructor": true, "decorators": [], "start_line": 9, "end_line": 14, "llm_score": null}
{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "LinkedList", "parent_classes": ["Generic[T]"], "docstring": "A singly linked list with O(1) prepend and O(n) append.", "source_code": "class LinkedList(Generic[T]):\n    \"\"\"A singly linked list with O(1) prepend and O(n) append.\"\"\"\n\n    def __init__(self):\n        self._head: Optional[Node[T]] = None\n        self._size: int = 0\n\n    def prepend(self, value: T) -> None:\n        \"\"\"Insert value at the front of the list.\"\"\"\n        self._head = Node(value, self._head)\n        self._size += 1\n\n    def append(self, value: T) -> None:\n        \"\"\"Insert value at the back of the list.\"\"\"\n        if self._head is None:\n            self._head = Node(value)\n            self._size += 1\n            return\n        cur = self._head\n        while cur.next:\n            cur = cur.next\n        cur.next = Node(value)\n        self._size += 1\n\n    def remove(self, value: T) -> bool:\n        \"\"\"Remove the first occurrence of value. Returns True if found.\"\"\"\n        if self._head is None:\n            return False\n        if self._head.value == value:\n            self._head = self._head.next\n            self._size -= 1\n            return True\n        cur = self._head\n        while cur.next:\n            if cur.next.value == value:\n                cur.next = cur.next.next\n                self._size -= 1\n                return True\n            cur = cur.next\n        return False\n\n    def __iter__(self) -> Iterator[T]:\n        \"\"\"Iterate over values in insertion order.\"\"\"\n        cur = self._head\n        while cur:\n            yield cur.value\n            cur = cur.next\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __repr__(self) -> str:\n        return \"LinkedList([\" + \", \".join(str(v) for v in self) + \"])\"", "skeleton": "class LinkedList(Generic[T]):\n    \"\"\"A singly linked list with O(1) prepend and O(n) append.\"\"\"\n\n    def __init__(self):\n        pass\n\n    def prepend(self, value: T) -> None:\n        \"\"\"Insert value at the front of the list.\"\"\"\n        pass\n\n    def append(self, value: T) -> None:\n        \"\"\"Insert value at the back of the list.\"\"\"\n        pass\n\n    def remove(self, value: T) -> bool:\n        \"\"\"Remove the first occurrence of value. Returns True if found.\"\"\"\n        pass\n\n    def __iter__(self) -> Iterator[T]:\n        \"\"\"Iterate over values in insertion order.\"\"\"\n        pass\n\n    def __len__(self) -> int:\n        pass\n\n    def __repr__(self) -> str:\n        pass\n", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": ["__init__", "prepend", "append", "remove", "__iter__", "__len__", "__repr__"], "method_count": 7, "has_constructor": true, "decorators": [], "start_line": 17, "end_line": 69, "llm_score": null}
{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "Stack", "parent_classes": ["Generic[T]"], "docstring": "LIFO stack backed by a Python list.", "source_code": "class Stack(Generic[T]):\n    \"\"\"LIFO stack backed by a Python list.\"\"\"\n\n    def __init__(self):\n        self._data: list[T] = []\n\n    def push(self, item: T) -> None:\n        \"\"\"Push item onto the stack.\"\"\"\n        self._data.append(item)\n\n    def pop(self) -> T:\n        \"\"\"Remove and return the top item. Raises IndexError if empty.\"\"\"\n        if not self._data:\n            raise IndexError(\"pop from empty stack\")\n        return self._data.pop()\n\n    def peek(self) -> T:\n        \"\"\"Return top item without removing it.\"\"\"\n        if not self._data:\n            raise IndexError(\"peek at empty stack\")\n        return self._data[-1]\n\n    def is_empty(self) -> bool:\n        return len(self._data) == 0\n\n    def __len__(self) -> int:\n        return len(self._data)", "skeleton": "class Stack(Generic[T]):\n    \"\"\"LIFO stack backed by a Python list.\"\"\"\n\n    def __init__(self):\n        pass\n\n    def push(self, item: T) -> None:\n        \"\"\"Push item onto the stack.\"\"\"\n        pass\n\n    def pop(self) -> T:\n        \"\"\"Remove and return the top item. Raises IndexError if empty.\"\"\"\n        pass\n\n    def peek(self) -> T:\n        \"\"\"Return top item without removing it.\"\"\"\n        pass\n\n    def is_empty(self) -> bool:\n        pass\n\n    def __len__(self) -> int:\n        pass\n", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": ["__init__", "push", "pop", "peek", "is_empty", "__len__"], "method_count": 6, "has_constructor": true, "decorators": [], "start_line": 72, "end_line": 98, "llm_score": null}
{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "Queue", "parent_classes": ["Generic[T]"], "docstring": "FIFO queue backed by a deque for O(1) enqueue and dequeue.", "source_code": "class Queue(Generic[T]):\n    \"\"\"FIFO queue backed by a deque for O(1) enqueue and dequeue.\"\"\"\n\n    def __init__(self):\n        from collections import deque\n        self._data: deque[T] = deque()\n\n    def enqueue(self, item: T) -> None:\n        \"\"\"Add item to the back of the queue.\"\"\"\n        self._data.append(item)\n\n    def dequeue(self) -> T:\n        \"\"\"Remove and return the front item. Raises IndexError if empty.\"\"\"\n        if not self._data:\n            raise IndexError(\"dequeue from empty queue\")\n        return self._data.popleft()\n\n    def front(self) -> T:\n        \"\"\"Return front item without removing it.\"\"\"\n        if not self._data:\n            raise IndexError(\"front of empty queue\")\n        return self._data[0]\n\n    def is_empty(self) -> bool:\n        return len(self._data) == 0\n\n    def __len__(self) -> int:\n        return len(self._data)", "skeleton": "class Queue(Generic[T]):\n    \"\"\"FIFO queue backed by a deque for O(1) enqueue and dequeue.\"\"\"\n\n    def __init__(self):\n        pass\n\n    def enqueue(self, item: T) -> None:\n        \"\"\"Add item to the back of the queue.\"\"\"\n        pass\n\n    def dequeue(self) -> T:\n        \"\"\"Remove and return the front item. Raises IndexError if empty.\"\"\"\n        pass\n\n    def front(self) -> T:\n        \"\"\"Return front item without removing it.\"\"\"\n        pass\n\n    def is_empty(self) -> bool:\n        pass\n\n    def __len__(self) -> int:\n        pass\n", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": ["__init__", "enqueue", "dequeue", "front", "is_empty", "__len__"], "method_count": 6, "has_constructor": true, "decorators": [], "start_line": 101, "end_line": 128, "llm_score": null}
{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "TreeNode", "parent_classes": ["Generic[T]"], "docstring": "A node in a binary search tree.", "source_code": "class TreeNode(Generic[T]):\n    \"\"\"A node in a binary search tree.\"\"\"\n    value: T\n    left: Optional[TreeNode[T]] = None\n    right: Optional[TreeNode[T]] = None", "skeleton": "class TreeNode(Generic[T]):\n    \"\"\"A node in a binary search tree.\"\"\"\n\n    pass", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": [], "method_count": 0, "has_constructor": false, "decorators": ["@dataclass"], "start_line": 132, "end_line": 136, "llm_score": null}
{"repo": "sieve-demo/data-structures", "file_path": "main.py", "language": "Python", "class_name": "BinarySearchTree", "parent_classes": ["Generic[T]"], "docstring": "Binary search tree with in-order traversal and iterative search.", "source_code": "class BinarySearchTree(Generic[T]):\n    \"\"\"Binary search tree with in-order traversal and iterative search.\"\"\"\n\n    def __init__(self):\n        self._root: Optional[TreeNode[T]] = None\n\n    def insert(self, value: T) -> None:\n        \"\"\"Insert a value into the BST.\"\"\"\n        if self._root is None:\n            self._root = TreeNode(value)\n            return\n        cur = self._root\n        while True:\n            if value < cur.value:\n                if cur.left is None:\n                    cur.left = TreeNode(value)\n                    return\n                cur = cur.left\n            else:\n                if cur.right is None:\n                    cur.right = TreeNode(value)\n                    return\n                cur = cur.right\n\n    def contains(self, value: T) -> bool:\n        \"\"\"Return True if value exists in the BST.\"\"\"\n        cur = self._root\n        while cur:\n            if value == cur.value:\n                return True\n            cur = cur.left if value < cur.value else cur.right\n        return False\n\n    def inorder(self) -> list[T]:\n        \"\"\"Return sorted list via in-order traversal.\"\"\"\n        result: list[T] = []\n\n        def _walk(node: Optional[TreeNode[T]]) -> None:\n            if node:\n                _walk(node.left)\n                result.append(node.value)\n                _walk(node.right)\n\n        _walk(self._root)\n        return result", "skeleton": "class BinarySearchTree(Generic[T]):\n    \"\"\"Binary search tree with in-order traversal and iterative search.\"\"\"\n\n    def __init__(self):\n        pass\n\n    def insert(self, value: T) -> None:\n        \"\"\"Insert a value into the BST.\"\"\"\n        pass\n\n    def contains(self, value: T) -> bool:\n        \"\"\"Return True if value exists in the BST.\"\"\"\n        pass\n\n    def inorder(self) -> list[T]:\n        \"\"\"Return sorted list via in-order traversal.\"\"\"\n        pass\n", "used_imports": ["from typing import Optional, Generic, TypeVar, Iterator"], "method_names": ["__init__", "insert", "contains", "inorder"], "method_count": 4, "has_constructor": true, "decorators": [], "start_line": 139, "end_line": 183, "llm_score": null}
{"repo": "sieve-demo/text-utils", "file_path": "main.py", "language": "Python", "class_name": "Tokenizer", "parent_classes": [], "docstring": "Simple whitespace tokenizer with optional lowercasing and stopword removal.", "source_code": "class Tokenizer:\n    \"\"\"\n    Simple whitespace tokenizer with optional lowercasing and stopword removal.\n    \"\"\"\n\n    def __init__(self, lowercase: bool = True, stopwords: Optional[set] = None):\n        self.lowercase = lowercase\n        self.stopwords = stopwords or set()\n\n    def tokenize(self, text: str) -> list[str]:\n        \"\"\"Split text into tokens, applying configured transforms.\"\"\"\n        if self.lowercase:\n            text = text.lower()\n        tokens = re.findall(r\"\\b\\w+\\b\", text)\n        if self.stopwords:\n            tokens = [t for t in tokens if t not in self.stopwords]\n        return tokens\n\n    def tokenize_batch(self, texts: list[str]) -> list[list[str]]:\n        \"\"\"Tokenize a list of texts.\"\"\"\n        return [self.tokenize(t) for t in texts]\n\n    def vocabulary(self, texts: list[str]) -> set[str]:\n        \"\"\"Return the full vocabulary across a list of texts.\"\"\"\n        vocab: set[str] = set()\n        for text in texts:\n            vocab.update(self.tokenize(text))\n        return vocab", "skeleton": "class Tokenizer:\n    \"\"\"Simple whitespace tokenizer with optional lowercasing and stopword removal.\"\"\"\n\n    def __init__(self, lowercase: bool = True, stopwords: Optional[set] = None):\n        pass\n\n    def tokenize(self, text: str) -> list[str]:\n        \"\"\"Split text into tokens, applying configured transforms.\"\"\"\n        pass\n\n    def tokenize_batch(self, texts: list[str]) -> list[list[str]]:\n        \"\"\"Tokenize a list of texts.\"\"\"\n        pass\n\n    def vocabulary(self, texts: list[str]) -> set[str]:\n        \"\"\"Return the full vocabulary across a list of texts.\"\"\"\n        pass\n", "used_imports": ["import re", "from typing import Optional"], "method_names": ["__init__", "tokenize", "tokenize_batch", "vocabulary"], "method_count": 4, "has_constructor": true, "decorators": [], "start_line": 87, "end_line": 114, "llm_score": null}
{"repo": "sieve-demo/text-utils", "file_path": "main.py", "language": "Python", "class_name": "TextCleaner", "parent_classes": [], "docstring": "Configurable text cleaning pipeline with chainable transforms.", "source_code": "class TextCleaner:\n    \"\"\"\n    Configurable text cleaning pipeline with chainable transforms.\n    \"\"\"\n\n    def __init__(self):\n        self._steps: list = []\n\n    def add_step(self, fn) -> TextCleaner:\n        \"\"\"Add a transform function to the pipeline. Returns self for chaining.\"\"\"\n        self._steps.append(fn)\n        return self\n\n    def clean(self, text: str) -> str:\n        \"\"\"Apply all pipeline steps in order.\"\"\"\n        for step in self._steps:\n            text = step(text)\n        return text\n\n    def clean_batch(self, texts: list[str]) -> list[str]:\n        \"\"\"Apply pipeline to a list of texts.\"\"\"\n        return [self.clean(t) for t in texts]\n\n    @classmethod\n    def default(cls) -> TextCleaner:\n        \"\"\"Return a cleaner with sensible defaults: whitespace normalization.\"\"\"\n        return cls().add_step(normalize_whitespace)", "skeleton": "class TextCleaner:\n    \"\"\"Configurable text cleaning pipeline with chainable transforms.\"\"\"\n\n    def __init__(self):\n        pass\n\n    def add_step(self, fn) -> TextCleaner:\n        \"\"\"Add a transform function to the pipeline. Returns self for chaining.\"\"\"\n        pass\n\n    def clean(self, text: str) -> str:\n        \"\"\"Apply all pipeline steps in order.\"\"\"\n        pass\n\n    def clean_batch(self, texts: list[str]) -> list[str]:\n        \"\"\"Apply pipeline to a list of texts.\"\"\"\n        pass\n\n    @classmethod\n    def default(cls) -> TextCleaner:\n        \"\"\"Return a cleaner with sensible defaults: whitespace normalization.\"\"\"\n        pass\n", "used_imports": [], "method_names": ["__init__", "add_step", "clean", "clean_batch", "default"], "method_count": 5, "has_constructor": true, "decorators": [], "start_line": 117, "end_line": 143, "llm_score": null}